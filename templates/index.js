const controller = require('../templates/functions/controllerTemplate')
const service = require('../templates/functions/serviceTemplate')
const route = require('../templates/functions/routeTemplate')
const index = require('../templates/functions/indexTemplate')

const TEMPLATES = {
  controller: controller,
  service: service,
  route: route,
  index: index
}

module.exports = TEMPLATES